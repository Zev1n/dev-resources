import { SectionContainer } from '@/components/SectionContainer';

export default function Contact() {
    return (
        <SectionContainer>
            <h1>Contato</h1>
        </SectionContainer>
    );
}
