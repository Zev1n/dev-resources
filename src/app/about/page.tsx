import { SectionContainer } from '@/components/SectionContainer';

export default function About() {
    return (
        <SectionContainer>
            <h1>Sobre nós</h1>
        </SectionContainer>
    );
}
